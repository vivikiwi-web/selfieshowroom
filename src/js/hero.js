/**
 * Hero entrance is CSS-only (see _hero.scss).
 * Kept as a no-op hook for future interactions.
 */
export function initHero() {
	// Intentionally empty — WPBakery strips inline scripts; CSS animations are reliable.
}
